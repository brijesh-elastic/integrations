---
description: Benchmark 1000 cloudflare_logpush.audit events ingested
data_stream:
  name: audit
corpora:
  generator:
    total_events: 1000
    template:
      type: gotext
      path: ./audit-benchmark/template.ndjson
    config:
      path: ./audit-benchmark/config.yml
    fields:
      path: ./audit-benchmark/fields.yml
